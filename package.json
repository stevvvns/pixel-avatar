{
  "name": "@stevvvns/pixel-avatar",
  "version": "0.0.1",
  "type": "module",
  "main": "dist/index.js",
  "files": [
    "dist/index.js",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "mkdir -p dist; cp src/index.html dist/; npx esbuild --bundle --format=esm --platform=browser --outdir=dist src/index.js",
    "example": "npm run build && npx serve ./dist; npx prettier src --write",
    "pub": "npm run build && npm publish --access=public",
    "lint": "npx eslint --fix src"
  },
  "author": "",
  "license": "MIT",
  "description": "",
  "dependencies": {
    "canvas": "^3.2.0",
    "seedrandom": "^3.0.5"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.0",
    "eslint": "^9.39.0",
    "globals": "^16.5.0"
  }
}
